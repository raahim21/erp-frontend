clone this project and go into config.js in the frontend folder
run this project locally by commenting out the line
// export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || "http://localhost:8000"; 
and uncomment the line 
export const API_BASE_URL = "http://localhost:8000";
then run npm run dev to start the project and login using username 'Arham' and password 123


make sure to also clone the erp-backend folder from the main repo, 
the tech stack used for this frontend is react
